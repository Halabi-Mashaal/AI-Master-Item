services:
  - type: web
    name: yamama-cement-final
    env: python
    buildCommand: pip install flask google-generativeai gunicorn
    startCommand: gunicorn --bind 0.0.0.0:$PORT FINAL_WORKING_APP:app
    plan: free
    branch: main
    healthCheckPath: /health
