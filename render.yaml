# Render Deployment Configuration

# Environment Variables
PYTHON_VERSION=3.11.5
BUILD_COMMAND=pip install --upgrade pip && pip install -r requirements.txt && python -m spacy download en_core_web_sm
START_COMMAND=gunicorn --bind 0.0.0.0:$PORT src.app:app --timeout 120 --workers 2

# For manual deployment
# 1. Set Python version to 3.11.5 in Render dashboard
# 2. Build command: pip install --upgrade pip && pip install -r requirements.txt && python -m spacy download en_core_web_sm
# 3. Start command: gunicorn --bind 0.0.0.0:$PORT src.app:app --timeout 120 --workers 2
# 4. Make sure to set environment variables for any API keys if needed
